services:
  backend:
    build: .  
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL="file:./dev.db"
      - JWT_SECRET="cristiano_ronaldo_7"
    volumes:
      - ./dev.db:/app/dev.db 

  frontend:
    image: node:20-alpine
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
    command: sh -c "npm install && npm run dev -- --host"